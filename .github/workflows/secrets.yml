name: Secrets

on:
 workflow_dispatch
env:
 MY_VAR: workflow
jobs:
 variables:
  env:
   MY_VAR: jobs
  runs-on: ubuntu-latest
  steps:
  - name: Print
    run: |
     echo ' ${{secrets.PASSWORD}}'
     echo ' ${{env.MY_VAR}}'
     echo ' ${{vars.MY_VAR}}'
    env:
     MY_VAR: steps
  
  
